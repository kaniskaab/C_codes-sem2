#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <strings.h>
char longPal(char a[]){
    int size= strlen(a);
    char temp[100];
    char rev[100];
    for(int i=0;i<size;i++){
        for(int j=0;j<temp;j++){
            temp[j]=a[j+i];

        }
    
    }

}